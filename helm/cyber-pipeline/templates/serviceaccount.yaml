apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "cyber-pipeline.serviceAccountName" . }}
  labels:
    {{- include "cyber-pipeline.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
