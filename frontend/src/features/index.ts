/**
 * Features index - barrel export for feature modules.
 *
 * IMPORTANT: Each feature module only exports its PUBLIC API.
 * Internal components, hooks, and utilities remain private to each feature.
 * This prevents accidental cross-feature coupling and keeps bundles small.
 *
 * Usage:
 *   import { CockpitPage } from '@/features/cockpit';
 *   import { JobDetailPage } from '@/features/jobs';
 */
