# ──────────────────────────────────────────────────────────────────────
# Secrets for Cyber Security Test Pipeline
# IMPORTANT: In production, use external secret management (Vault, AWS Secrets Manager, etc.)
# These are base64 encoded placeholders - replace with actual secrets!
# ──────────────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: cyber-pipeline-secrets
  namespace: cyber-pipeline
  labels:
    app.kubernetes.io/name: cyber-pipeline
    app.kubernetes.io/component: secrets
type: Opaque
data:
  # echo -n 'your-secret-key' | base64
  # Replace these with actual base64-encoded secrets
  APP_SECRET_KEY: REPLACE_ME_BASE64
  REDIS_PASSWORD: REPLACE_ME_BASE64
  REDIS_SENTINEL_PASSWORD: REPLACE_ME_BASE64
